#include<stdio.h>
int main()
{
    char arr[20];
    scanf("%s",&arr);
    for(int i=0;arr[i]!='\0';i++)
    {
        arr[i]=2*arr[i]-48;//2*arr[i]所表示的数的asc码值
    }
    printf("%s\n",arr);
    return 0;
}
